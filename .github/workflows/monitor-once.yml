name: Monitor booking slots once

on:
  workflow_dispatch:
    inputs:
      allow_booking:
        description: Allow auto_book_if_capacity jobs to submit reservations
        required: true
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: monitor-once
  cancel-in-progress: false

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      FUDAN_USERNAME: ${{ secrets.FUDAN_USERNAME }}
      FUDAN_PASSWORD: ${{ secrets.FUDAN_PASSWORD }}
      FUDAN_TOTP_SECRET: ${{ secrets.FUDAN_TOTP_SECRET }}
      FUDAN_MOBILE: ${{ secrets.FUDAN_MOBILE }}
      FUDAN_NODE_BIN: node
      QQ_SMTP_USERNAME: ${{ secrets.QQ_SMTP_USERNAME }}
      QQ_SMTP_AUTH_CODE: ${{ secrets.QQ_SMTP_AUTH_CODE }}
      NOTIFICATION_EMAIL: ${{ secrets.NOTIFICATION_EMAIL }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
      - uses: pnpm/action-setup@v4
        with:
          version: 11.25.0
      - run: pnpm install --frozen-lockfile
        working-directory: challenge
      - run: python -m pip install -e .
      - name: Check configured slots and notify
        run: |
          if [[ -z "$FUDAN_USERNAME" || -z "$FUDAN_PASSWORD" ]]; then
            echo "FUDAN_USERNAME and FUDAN_PASSWORD are required." >&2
            exit 2
          fi
          args=(monitor-once --config config/config.example.yaml)
          if [[ "${{ inputs.allow_booking }}" == "true" ]]; then
            args+=(--allow-booking)
          fi
          fudan-booking "${args[@]}"
